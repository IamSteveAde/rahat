'use client';import {useState} from 'react';import {CalendarDays,Users,Search,MapPin} from 'lucide-react';import {useRouter} from 'next/navigation'
export function BookingSearch(){const r=useRouter();const today=new Date().toISOString().slice(0,10);const[checkIn,setCheckIn]=useState(today);const[checkOut,setCheckOut]=useState(new Date(Date.now()+86400000).toISOString().slice(0,10));const[guests,setGuests]=useState(2);return <div className="rounded-3xl bg-white p-3 text-black shadow-luxury md:p-4"><div className="grid md:grid-cols-[1.4fr_1fr_1fr_.8fr_auto] md:items-end"><div className="border-b p-3 md:border-b-0 md:border-r"><label className="flex items-center gap-2 text-[11px] font-bold uppercase tracking-widest text-black/50"><MapPin size={14}/>Where</label><p className="mt-2 text-sm font-semibold">Rahat Luxury Apartment, Ikota</p></div><div className="border-b p-3 md:border-b-0 md:border-r"><label className="flex items-center gap-2 text-[11px] font-bold uppercase tracking-widest text-black/50"><CalendarDays size={14}/>Check-in</label><input min={today} value={checkIn} onChange={e=>setCheckIn(e.target.value)} type="date" className="mt-2 w-full bg-transparent text-sm font-semibold outline-none"/></div><div className="border-b p-3 md:border-b-0 md:border-r"><label className="flex items-center gap-2 text-[11px] font-bold uppercase tracking-widest text-black/50"><CalendarDays size={14}/>Check-out</label><input min={checkIn} value={checkOut} onChange={e=>setCheckOut(e.target.value)} type="date" className="mt-2 w-full bg-transparent text-sm font-semibold outline-none"/></div><div className="p-3"><label className="flex items-center gap-2 text-[11px] font-bold uppercase tracking-widest text-black/50"><Users size={14}/>Guests</label><select value={guests} onChange={e=>setGuests(+e.target.value)} className="mt-2 w-full bg-transparent text-sm font-semibold outline-none"><option>1</option><option>2</option><option>3</option><option>4</option></select></div><button onClick={()=>r.push(`/availability?checkIn=${checkIn}&checkOut=${checkOut}&guests=${guests}`)} className="flex h-14 items-center justify-center gap-2 rounded-2xl bg-black px-6 font-semibold text-white hover:bg-gold-900"><Search size={18}/>Search</button></div></div>}

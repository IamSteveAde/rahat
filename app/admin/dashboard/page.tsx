import {AdminShell} from '@/components/admin/AdminShell';import {demoBookings,apartments,formatNaira} from '@/lib/data';export default function Dashboard(){const revenue=demoBookings.reduce((s,b)=>s+b.total,0);return <AdminShell title="Dashboard"><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[['Revenue',formatNaira(revenue)],['Upcoming Bookings',String(demoBookings.length)],['Occupancy','70%'],['Available Units',String(apartments.length-1)]].map(([a,b])=><div className="rounded-2xl bg-white p-5 shadow-sm" key={a}><p className="text-xs font-bold uppercase tracking-wider text-black/40">{a}</p><p className="mt-3 text-3xl font-semibold">{b}</p></div>)}</div><div className="mt-6 grid gap-6 lg:grid-cols-[1.4fr_1fr]"><div className="rounded-2xl bg-white p-6"><h2 className="display text-2xl">Recent bookings</h2><div className="mt-5 divide-y">{demoBookings.map(b=><div className="flex items-center justify-between py-4" key={b.id}><div><p className="font-semibold">{b.apartmentName}</p><p className="text-xs text-black/45">{b.guestName} · {b.checkIn} → {b.checkOut}</p></div><span className="rounded-full bg-gold-100 px-3 py-1 text-xs">{b.bookingStatus}</span></div>)}</div></div><div className="rounded-2xl bg-black p-6 text-white"><h2 className="display text-2xl">Inventory pulse</h2><div className="mt-5 space-y-3">{apartments.map(a=><div key={a.slug} className="flex justify-between border-b border-white/10 pb-3 text-sm"><span>{a.name}</span><span className="text-gold-300">{a.slug==='monica'?'Booked':'Available'}</span></div>)}</div></div></div></AdminShell>}

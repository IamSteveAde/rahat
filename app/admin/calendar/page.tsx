import {AdminShell} from '@/components/admin/AdminShell';import {apartments} from '@/lib/data';export default function Calendar(){const days=Array.from({length:12},(_,i)=>12+i);return <AdminShell title="Availability calendar"><div className="overflow-x-auto rounded-2xl bg-white p-5"><div className="min-w-[900px]"><div className="grid grid-cols-[180px_repeat(12,1fr)] border-b pb-3 text-xs font-bold text-black/40"><div>Apartment</div>{days.map(d=><div key={d}>{d}</div>)}</div>{apartments.map((a,i)=><div className="grid grid-cols-[180px_repeat(12,1fr)] border-b last:border-0" key={a.slug}><div className="py-4 text-sm font-semibold">{a.name}</div>{days.map(d=>{const booked=(a.slug==='monica'&&d<=14)||(a.slug==='irene'&&d>=16&&d<18)||(a.slug==='alexa'&&d>=19);return <div className="border-l p-1" key={d}><div className={`h-8 rounded-md ${booked?'bg-gold-400':'bg-black/5'}`} title={booked?'Booked':'Available'}/></div>})}</div>)}</div></div></AdminShell>}
